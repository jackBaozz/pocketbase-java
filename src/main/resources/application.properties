# Safe defaults bundled into the JAR/native image. External config files override these values.
app.name=pocketbase-java
# app.profile=dev
server.host=127.0.0.1
server.port=8090
server.data-dir=pb_data
storage.type=sqlite
