name: Native Release Build

permissions:
  contents: write

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag name (e.g. v0.3.0). Leave empty to only upload build artifacts without publishing a Release.'
        required: false
        default: ''
  push:
    tags: [ "v*" ]

jobs:
  native:
    name: Build (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: windows-amd64
            os: windows-latest
            asset_suffix: windows-amd64
    steps:
      - uses: actions/checkout@v5

      - name: Resolve Version
        id: version
        shell: bash
        run: |
          VERSION="${{ github.event.inputs.release_tag || github.ref_name }}"
          VERSION="${VERSION#v}"
          if [ -z "$VERSION" ]; then
            VERSION=$(grep -m1 -oP '<version>\K[^<]+' pom.xml | head -n1)
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Building version: $VERSION"

      - name: Setup GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '25'
          distribution: 'graalvm'
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Build Admin UI
        shell: bash
        run: |
          cd UI
          npm ci
          npm run build

      - name: Build Native Image
        shell: bash
        run: mvn -gs settings.xml -s settings.xml -Pnative clean package -DskipTests

      - name: Stage Binary
        shell: bash
        run: |
          mkdir -p dist
          cp target/pocketbase-java.exe dist/pocketbase-java-${{ matrix.asset_suffix }}-v${{ steps.version.outputs.version }}.exe

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: pocketbase-java-${{ matrix.asset_suffix }}-v${{ steps.version.outputs.version }}
          path: dist/pocketbase-java-*

  release:
    name: Publish GitHub Release
    if: startsWith(github.ref, 'refs/tags/v') || github.event.inputs.release_tag != ''
    needs: native
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.release_tag || github.ref_name }}
          files: dist/*/*
          fail_on_unmatched_files: true
          generate_release_notes: true
