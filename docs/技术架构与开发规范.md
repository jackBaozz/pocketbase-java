# pocketbase-java 技术架构与开发规范

## 1. 项目简介

`pocketbase-java` 是一个面向 PocketBase HTTP API 的轻量级 Java SDK。项目目标是为 Java 应用提供稳定、低依赖、可测试、易扩展的 PocketBase 客户端基础能力。

本项目参考 `springboot-graalvm-demo` 的工程组织方式，保留明确的技术栈说明、目录结构、构建命令、编码约束和测试要求。但本项目是 **SDK 库**，不是 Spring Boot Web 应用，因此不引入 Spring Runtime，也不绑定特定 Web 容器。

---

## 2. 核心技术栈

| 类别 | 技术 | 版本 / 说明 |
| --- | --- | --- |
| 编程语言 | Java | 17+ |
| HTTP 客户端 | JDK HttpClient | `java.net.http.HttpClient` |
| JSON 处理 | Jackson | `jackson-databind` + `jackson-datatype-jsr310` |
| 构建工具 | Maven | 3.9+ |
| 测试框架 | JUnit Jupiter | JUnit 5 |
| CI | GitHub Actions | JDK 17 / 21 / 25 矩阵 |

---

## 3. 架构特性

### 3.1 SDK 分层

项目采用轻量分层，不引入复杂框架：

- **Client 层** (`PocketBaseClient`): 维护基础 URL、HTTP 客户端、JSON 序列化、认证状态和通用请求发送逻辑。
- **Service 层** (`CollectionService`, `CollectionsService`): 按 PocketBase 资源域封装 API。
- **Model 层** (`AuthResponse`, `RecordList`, `PocketBaseError`): 描述 PocketBase 常用响应结构。
- **Options 层** (`ListOptions`, `RecordQuery`): 描述请求查询参数，避免调用方手写散乱 Map。
- **Exception 层** (`PocketBaseException`): 统一包装 HTTP 错误和传输错误。

### 3.2 低依赖设计

SDK 不直接依赖 Spring、OkHttp、Retrofit 等框架，避免把使用方锁死在特定生态内。HTTP 由 JDK 原生能力完成，运行时只引入 Jackson。

### 3.3 GraalVM / Native Image 友好约束

虽然本项目不默认执行 Native Image 编译，但核心设计遵循 GraalVM 友好原则：

| 特性 | 约束说明 |
| --- | --- |
| 反射 | 不主动使用动态反射；JSON 映射集中由 Jackson 完成。 |
| 动态代理 | 不使用 JDK 动态代理或 CGLIB。 |
| 资源加载 | 核心运行路径不依赖 classpath 动态资源扫描。 |
| 第三方依赖 | 新增运行时依赖前必须评估 Native Image 兼容性和依赖体积。 |

---

## 4. 当前功能范围

| 模块 | 类 | 能力 |
| --- | --- | --- |
| 记录 API | `CollectionService` | list / getOne / create / update / delete |
| 认证 API | `CollectionService` | authWithPassword / listAuthMethods |
| 集合 API | `CollectionsService` | list / getOne / create / update / delete |
| 认证状态 | `AuthStore` | token 保存、清理、自动注入 Bearer 请求头 |
| 错误处理 | `PocketBaseException` | HTTP 状态码、请求方法、URI、响应体、错误结构 |

---

## 5. 目录结构

```text
pocketbase-java/
├── docs/
│   ├── API设计.md
│   └── 技术架构与开发规范.md
├── src/
│   ├── main/
│   │   └── java/io/github/jackbaozz/pocketbase/
│   │       ├── AuthResponse.java
│   │       ├── AuthStore.java
│   │       ├── CollectionService.java
│   │       ├── CollectionsService.java
│   │       ├── ListOptions.java
│   │       ├── PocketBaseClient.java
│   │       ├── PocketBaseError.java
│   │       ├── PocketBaseException.java
│   │       ├── RecordList.java
│   │       └── RecordQuery.java
│   └── test/
│       └── java/io/github/jackbaozz/pocketbase/
│           └── PocketBaseClientTest.java
├── .github/workflows/ci.yml
├── .editorconfig
├── .gitignore
├── .java-version
├── CHANGELOG.md
├── CONTRIBUTING.md
├── LICENSE
├── README.md
├── pom.xml
└── settings.xml
```

---

## 6. 开发环境要求

### 6.1 JDK

- 最低版本: JDK 17。
- 推荐版本: JDK 21 或 Oracle GraalVM 25。
- `.java-version` 固定为 `17`，保证 SDK 面向使用方的最低兼容性。

### 6.2 Maven

- Maven 3.9+。
- 项目不包含 Maven Wrapper，默认使用本机全局 Maven。
- 如果中央仓库访问不稳定，使用项目内 `settings.xml`:

```bash
mvn -gs settings.xml -s settings.xml test
```

---

## 7. 构建、测试与验证

### 7.1 运行测试

```bash
mvn -gs settings.xml -s settings.xml test
```

### 7.2 打包

```bash
mvn -gs settings.xml -s settings.xml clean package
```

打包产物包括：

- `target/pocketbase-java-0.1.0-SNAPSHOT.jar`
- `target/pocketbase-java-0.1.0-SNAPSHOT-sources.jar`
- `target/pocketbase-java-0.1.0-SNAPSHOT-javadoc.jar`

### 7.3 本地安装

```bash
mvn -gs settings.xml -s settings.xml clean install
```

---

## 8. 编码规范

### 8.1 API 设计规范

- 公共 API 必须保持小而稳定，新增能力优先通过新增方法，不随意改变已有方法语义。
- 请求参数使用 Options 对象封装，避免公共方法暴露散乱字符串 Map。
- 任意远程 API 错误必须保留原始响应体，方便排查 PocketBase 后端校验失败。
- SDK 不吞异常；非 2xx 响应统一抛 `PocketBaseException`。

### 8.2 命名规范

- 包名: `io.github.jackbaozz.pocketbase`。
- Client: 以 `Client` 结尾。
- 资源服务: 以 `Service` 结尾。
- 查询参数: 以 `Options` 或 `Query` 结尾。
- 远端响应结构: 使用名词，如 `RecordList`、`AuthResponse`。

### 8.3 依赖规范

- 新增运行时依赖前必须回答三个问题：
  1. 是否能用 JDK 标准库完成？
  2. 是否会显著增加传递依赖？
  3. 是否影响 GraalVM Native Image 兼容性？
- 测试依赖可以适度引入，但当前优先使用 JDK 内置 `HttpServer` 做 HTTP 行为测试。

---

## 9. 测试要求

新增或修改 SDK 行为时，至少覆盖：

- URL path 和 query 参数构造。
- JSON 请求体序列化。
- 认证 token 保存和自动注入。
- 非 2xx 响应异常映射。
- 空参数、非法参数的本地校验。

测试应验证真实 HTTP 请求行为，而不是只测纯对象拼装。

---

## 10. 后续扩展计划

- 文件上传与 multipart 支持。
- Realtime SSE 订阅。
- 自定义 POJO record 映射。
- OAuth2 auth helpers。
- Batch API。
- Maven Central 发布配置。
